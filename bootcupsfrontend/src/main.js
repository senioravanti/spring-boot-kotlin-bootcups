import './assets/main.scss'

import { createApp } from 'vue'
import App from './App.vue'

// Точка входа в приложение на vue.js
// `npm run dev` -> Запустить приложение
// id элемента div в файле index.html
createApp(App).mount('#app')